#Programa onde será realizado o calculo para ver se o aluno foi aprovado ou reprovado

nota1 = float(input("Digite a sua primeira nota: "))
nota2 = float(input("\nDigite a sua segunda nota: "))

if (nota1 + nota2)/2 >= 7:
	print("\nAluno aprovado")
elif (nota1 + nota2)/2 >= 5:
	print("\nAluno de exame")
else:
	print("\n5Aluno reprovado")
#teste